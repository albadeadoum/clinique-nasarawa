{{ form_start(form, {
    attr: {
        id: 'editHospitalisationForm'
    }
}) }}

{{ form_widget(form) }}

<div class="mt-3 text-end">
    <button class="btn btn-success">Enregistrer</button>
</div>

{{ form_end(form) }}